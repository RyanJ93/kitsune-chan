'use strict';

module.exports = {
    controllers: require('./controllers'),
    models: require('./models'),
    providers: require('./providers'),
    services: require('./services'),
    support: require('./support'),
    CommandRouter: require('./CommandRouter')
};
